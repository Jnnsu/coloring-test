// import getGPTResponse from '@/api/getGPTResponse';

// // 기존에 설정한 프롬프트
// const promptA = '오늘의 날씨는 ';

// // 사용자가 발화한 값
// const userSpeech = '맑음'; // 실제로는 음성인식 결과나 입력 필드에서 가져온 값이 될 것임

// const res = getGPTResponse(promptA, userSpeech);
// console.log(`프롬프트1 결과: ${res}`);
